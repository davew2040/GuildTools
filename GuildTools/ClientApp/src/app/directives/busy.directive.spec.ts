import { BusyDirective } from './busy.directive';

describe('BusyDirectiveDirective', () => {
  it('should create an instance', () => {
    //const directive = new BusyDirective();
    //expect(directive).toBeTruthy();
  });
});
